# Beautiful-Mermaid Skill - Python Dependencies
# This skill requires no additional Python packages (uses only standard library)

# System Dependencies:
# - Node.js >= 14.0.0
# - npm or yarn

# Installation:
# 1. npm install -g beautiful-mermaid@^0.1.3
# 2. Ensure Node.js and npm are installed on your system

# No Python packages are required for this skill to function.
# All rendering is handled by the Node.js beautiful-mermaid library via subprocess calls.
